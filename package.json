{
  "name": "fish-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:contracts": "npm run build --workspace=packages/contracts",
    "build:backend": "npm run build:contracts && npm run build --workspace=apps/backend",
    "build:frontend": "npm run build:contracts && npm run build --workspace=apps/frontend",
    "start:backend": "npm run start --workspace=apps/backend",
    "dev:backend": "npm run dev --workspace=apps/backend",
    "dev:frontend": "npm run dev --workspace=apps/frontend",
    "dev:mobile": "npm run start --workspace=apps/mobile"
  }
}
